# ── Secrets / environment ────────────────────────────────────────────
.env
.env.*
!.env.example
*.pem
*.key

# ── Rust (mcp-shield) ────────────────────────────────────────────────
target/
**/target/
# Runtime-generated trusted-schema store
baseline_hashes.json

# ── Python (vector-anchor, trace-audit) ──────────────────────────────
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
.venv/
venv/
# Runtime-generated data / models
chroma_store/
**/chroma_store/
baseline_distribution.json
.ollama/

# ── Node / Next.js (dashboard) ───────────────────────────────────────
node_modules/
.next/
next-env.d.ts
npm-debug.log*

# ── Local-only agent guidance ────────────────────────────────────────
CLAUDE.md

# ── Editor / OS ──────────────────────────────────────────────────────
.DS_Store
.idea/
.vscode/
*.swp

# ── Local presentation material (not part of the codebase) ───────────
AEOS_Guard_Project_Overview.pdf
build_aeos_pdf.py
_aeos_p*.png

# ── Generated benchmark reports (per-run artifacts, uploaded not stored) ──
**/fixtures/benchmark_results.json

# ── Local Compose overrides (e.g. an alternate host port) ────────────
docker-compose.override.yml

fastapi>=0.110
uvicorn[standard]>=0.52.0
pydantic>=2.6
chromadb>=0.5
# ChromaDB requests uvicorn[standard]. Declare its Linux-only event loop
# explicitly so the hashed lock remains complete when compiled on Windows.
uvloop>=0.22.1; sys_platform != "win32"
